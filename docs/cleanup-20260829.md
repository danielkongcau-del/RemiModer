# Workspace cleanup — 2026-08-29

The repository was reduced to the generic UnifiedCapture platform and public documentation.

Removed obsolete implementation families:

- the retired Unity project;
- the retired Godot and Blender prototypes;
- the speculative ability-kernel implementation and its generated build output;
- the Unity animation-fix implementation;
- superseded single-purpose runtime observers and intermediate observer reviews;
- obsolete harness object files and historical scratch generations.

Target-derived notes were retained locally under an ignored `local-only/` tree. Target installations, extracted evidence, source assets, current external tools, and the active UnifiedCapture build/runtime files were preserved locally but excluded from Git.

Tracked files from the retired implementation remain recoverable from earlier Git history. Untracked caches and prototype directories removed during this cleanup are not recoverable from this commit.
